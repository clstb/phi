serve:
  proxy:
    port: 4455
  api:
    port: 4456

access_rules:
  repositories:
    - file:////home/ory/rules.yaml

log:
  level: debug

errors:
  fallback:
    - json
  handlers:
    redirect:
      enabled: false
    json:
      enabled: true
      config:
        verbose: true

mutators:
  noop:
    enabled: true
  id_token:
    enabled: true
    config:
      issuer_url: http://localhost:4455
      jwks_url: file:///home/ory/jwks.json
      claims: |
        {
          "session": {{ .Extra | toJson }}
        }

authenticators:
  anonymous:
    enabled: true
  noop:
    enabled: true
  bearer_token:
    enabled: true
    config:
      check_session_url: https://intelligent-carver-9wwhrnif5g.projects.oryapis.com/api/kratos/public/sessions/whoami
      preserve_path: true
      extra_from: "@this"
      subject_from: "identity.id"


authorizers:
  allow:
    enabled: true
  deny:
    enabled: true
